# 📱 11. ระบบ Barcode Scanner

## ✅ Checklist

### Barcode ในสินค้า

- [ ] ช่อง Barcode ในหน้าเพิ่มสินค้า (`/products/create`)
- [ ] ช่อง Barcode ในหน้าแก้ไขสินค้า (`/products/{id}/edit`)
- [ ] ปุ่ม "สร้างอัตโนมัติ" สร้าง Barcode 12 หลัก
- [ ] Barcode ไม่ซ้ำกัน (Unique)

### Barcode Scanner ใน POS

- [ ] ช่องสแกน Barcode ในหน้า POS (`/pos`)
- [ ] Auto-focus ที่ช่อง Barcode
- [ ] สแกน Barcode → เพิ่มสินค้าเข้าตะกร้าทันที
- [ ] กด Enter → ค้นหาสินค้า
- [ ] แสดง Error ถ้าไม่พบสินค้า
- [ ] Focus กลับไปที่ช่อง Barcode หลังสแกน

### Hardware

- [ ] รองรับเครื่องยิง Barcode USB
- [ ] รองรับเครื่องยิง Barcode Bluetooth
- [ ] ทดสอบกับ iTCAN Scanner

---

## 📁 ไฟล์ที่เกี่ยวข้อง

### Controllers

- `app/Http/Controllers/POSController.php` (getProductByBarcode)

### Views

- `resources/js/Pages/Products/Create.vue` (Barcode field)
- `resources/js/Pages/Products/Edit.vue` (Barcode field)
- `resources/js/Pages/POS.vue` (Barcode Scanner section)

### Routes

- `GET /pos/barcode/{barcode}` → ค้นหาสินค้าด้วย Barcode

### Database

- `products.barcode` (VARCHAR 50, UNIQUE, NULLABLE)

---

## 🧪 วิธีทดสอบ

### ทดสอบสร้าง Barcode

1. ไปที่ `/products/create`
2. กดปุ่ม "สร้างอัตโนมัติ"
3. ตรวจสอบว่า Barcode 12 หลักถูกสร้าง
4. บันทึกสินค้า

### ทดสอบแก้ไข Barcode

1. ไปที่ `/products/{id}/edit`
2. ตรวจสอบว่า Barcode แสดง
3. กดปุ่ม "สร้างอัตโนมัติ" เพื่อสร้างใหม่
4. บันทึก

### ทดสอบสแกนใน POS

1. เสียบเครื่อง iTCAN
2. ไปที่ `/pos`
3. ยิง Barcode ที่สินค้า
4. ตรวจสอบว่าสินค้าเพิ่มเข้าตะกร้า
5. ยิง Barcode ซ้ำ → จำนวนเพิ่มขึ้น

### ทดสอบ Barcode ไม่พบ

1. พิมพ์ Barcode ที่ไม่มีในระบบ
2. กด Enter
3. ตรวจสอบว่าแสดง Error "ไม่พบสินค้า"

---

## 📝 หมายเหตุ

- Barcode สร้างจาก: Timestamp (10 หลัก) + Random (2 หลัก)
- เครื่องยิง Barcode ทำงานเหมือน Keyboard
- ยิง Barcode → พิมพ์ตัวเลข + กด Enter อัตโนมัติ
- รองรับ Barcode ทุกรูปแบบ (EAN-13, Code128, QR Code)
